# whisperboy — main build plan

## Status: 🟡 PRE-PHASE 0 (skeleton + plans only)

_Repo skeleton, README, CLAUDE.md, and this plan landed in the initial commit. No code yet. Phase 0 is the first phase that actually does anything._

## Stack (locked)

- **Language:** Kotlin
- **UI:** Jetpack Compose
- **Audio:** androidx.media3 (ExoPlayer + MediaSession + **`MediaLibraryService`** — see CLAUDE.md for why `MediaLibraryService` rather than `MediaSessionService`)
- **Data:** Room (book / chapter / bookmark cache), DataStore (preferences), SAF / `DocumentFile` (audiobook files — no MediaStore, no `READ_EXTERNAL_STORAGE`)
- **Build front-end:** Google's [Android CLI](https://developer.android.com/tools/agents/android-cli) (`android` command, launched April 2026). Wraps Gradle, SDK, install, run. **No Android Studio.**
- **Build back-end:** Gradle (driven via the Android CLI). The repo includes a Gradle wrapper.
- **Unit tests:** Robolectric (JVM-only, zero device).
- **UI tests:** [mobile-mcp](https://github.com/mobile-next/mobile-mcp) over ADB. Current target: headless AVD `medium_phone` (Android 16 / API 36, RSS ~3.2 GB) — see Phase 0.
- **Knowledge:** `android docs search` for live Android API guidance. `android-skills-mcp` for the official Android skills inside Claude Code.

## Spiritual sibling

[Voice](https://github.com/PaulWoitaschek/Voice) (GPLv3, by Paul Woitaschek) is the closest functional analog. Whisperboy is **not** a fork — written from scratch in Kotlin + Compose, MIT-licensed — but Voice's mapped design space is the one we start in:

- SAF-only library, four folder modes (`SingleFile` / `SingleFolder` / `Root` / `Author`)
- Per-book speed, skip-silence, gain stored on the `BookContent` row
- Sleep timer with fade-out, shake-to-resume, end-of-chapter mode, auto-bookmark on fire
- Custom MP4 box parser for M4B chapter markers
- `MediaLibraryService` for Android Auto

When a phase below has a Voice analog, the phase header references the Voice module that solved that problem so a subagent can study prior art before inventing.

---

## Phase 0 — prerequisites (one-time, on the host)

Goal: a buildable host. These run once per developer machine. Tracked here so the environment is verifiable before agents go to work. (Same machine as tonearmboy, so most of these are no-ops on this user's box, but we tick them for completeness on a fresh checkout.)

- [x] **0.1** Install Google's Android CLI: `curl -fsSL https://dl.google.com/android/cli/latest/linux_x86_64/android -o ~/.local/bin/android && chmod +x ~/.local/bin/android`. — already installed at `~/.local/bin/android`, version `0.7.15326717`.
- [x] **0.2** `android sdk install platforms/android-34 build-tools/34.0.0` — installs to `~/Android/Sdk/`. — `~/Android/Sdk/platforms/` has `android-34` and `android-36`; `~/Android/Sdk/build-tools/` has `34.0.0` and `36.0.0` (carried from tonearmboy; whisperboy compiles against API 36 like tonearmboy does).
- [x] **0.3** JDK 21 bundled by the Android CLI is sufficient for AGP 9. System Java only matters if a subagent invokes `./gradlew` directly without going through `android` — set `JAVA_HOME` to a system JDK 17+ in that case (see CLAUDE.md). — `/usr/lib/jvm/java-17-openjdk` and `/usr/lib/jvm/java-26-openjdk` both available; CLAUDE.md guidance points at `java-26-openjdk` for direct `./gradlew` invocations.
- [x] **0.4** `mobile` MCP server registered at **project scope** (`whisperboy/.mcp.json`) — already committed in the initial skeleton; verify with `claude mcp list` from inside the repo. — entry confirmed in `.mcp.json` shipped with `a863c13`.
- [x] **0.5** `android-skills` MCP server registered at **project scope** (`whisperboy/.mcp.json`) — already committed in the initial skeleton; verify with `claude mcp list` from inside the repo. — entry confirmed in `.mcp.json` shipped with `a863c13`.
- [x] **0.6** Test target: **headless AVD `medium_phone`** (shared with tonearmboy). Created via `android emulator create --profile=medium_phone`. Started headlessly. Visible to ADB as `emulator-5554`. — `emulator -list-avds` confirms `medium_phone` exists; `adb devices` shows `emulator-5554 device` already running (carried from tonearmboy session).

**Shipped:** 0.1–0.6 in commit _(this commit)_.

---

## Phase A — scaffold

Goal: a buildable, sideload-able APK that boots into a blank Compose screen. Everything that follows assumes this exists. Mirror tonearmboy Phase A almost exactly — same template choice, same gradle setup, same package convention.

- [x] **A.0** Browse `android create list` and pick the closest official template. Default expectation: `empty-activity` (the same template tonearmboy used). — chose `empty-activity` (only template currently shipped, tagged `compose,activity,agp-9`); same call tonearmboy made.
- [x] **A.1** `android create --name=whisperboy --output=. <template>` from inside the repo root. Verify the generated layout: `app/`, `gradle/wrapper/`, `settings.gradle.kts`, `build.gradle.kts`, `gradle.properties`. Rename package from `com.example.whisperboy` to `com.eight87.whisperboy`. Rename theme to `WhisperboyTheme`. — scaffolded into `/tmp/whisperboy-scaffold` then `rsync --ignore-existing` into the repo (preserves README, CLAUDE.md, plans, LICENSE, .gitignore, .mcp.json). Package renamed `com.example.whisperboy` → `com.eight87.whisperboy` everywhere via `sed`; source dirs moved `app/src/{main,test,androidTest}/java/com/example/whisperboy` → `app/src/.../java/com/eight87/whisperboy`. Theme renamed `MyApplicationTheme` → `WhisperboyTheme` and `Theme.MyApplication` → `Theme.Whisperboy`. Template ships with Compose + Material 3 + AGP 9.0.1 + Kotlin 2.3.20 + Navigation 3 + Lifecycle ViewModel + a placeholder `DataRepository` / `MainScreenViewModel` / `MainScreen` — kept as-is for the hello-Compose smoke; will be reshaped or stripped as Phase B / E land their own surfaces.
- [x] **A.2** Add Media3 deps to `app/build.gradle.kts`: `media3-exoplayer`, `media3-session`, `media3-ui`. Pin via a single `media3 = "1.10.0"` key in `libs.versions.toml` shared by all three module entries (no Maven BOM exists for Media3). — single `media3 = "1.10.0"` key in `[versions]`, three `androidx-media3-{exoplayer,session,ui}` entries in `[libraries]`, three `implementation(libs.androidx.media3.*)` lines in `app/build.gradle.kts`. A bump touches one line.
- [x] **A.3** `AndroidManifest.xml` adds: `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_MEDIA_PLAYBACK`, `POST_NOTIFICATIONS`, `WAKE_LOCK`. **No `READ_MEDIA_AUDIO` / `READ_EXTERNAL_STORAGE`** — whisperboy is SAF-only. `minSdk = 28` (matching Voice's floor; SAF flake on older APIs makes the cost of supporting 26-27 not worth the user count). `compileSdk` and `targetSdk` set to current stable. — four `<uses-permission>` lines added with a comment block pointing at CLAUDE.md "Why SAF (not MediaStore)". `minSdk` bumped from template default 24 → 28. `compileSdk = 36`, `targetSdk = 36` kept from template (matches the API-36 system image on `medium_phone`).
- [x] **A.4** `.gitignore` covers `build/`, `.gradle/`, `local.properties`, `captures/`, `.cxx/`, `*.apk`, `*.aab`, `keystore.properties`, `*.keystore`, `*.jks`, plus `.idea/`, `*.iml`. (Already committed in skeleton — verify.) — root `.gitignore` from `a863c13` already covers all required entries; scaffold's `app/.gitignore` (`/build` only) kept as the per-module file.
- [x] **A.5** Build verification: `./gradlew assembleDebug` succeeds. APK lands at `app/build/outputs/apk/debug/whisperboy-debug.apk`. — `JAVA_HOME=/usr/lib/jvm/java-26-openjdk ANDROID_HOME=$HOME/Android/Sdk ./gradlew :app:assembleDebug` → `BUILD SUCCESSFUL in 18s`. APK 39 MB (template carries Navigation 3 + Lifecycle ViewModel — will trim once Phase B/E reshape the entry surface).
- [x] **A.6** Install verification: `android run --apks=app/build/outputs/apk/debug/whisperboy-debug.apk` launches the placeholder activity on `emulator-5554`. Confirm via `dumpsys window | grep mCurrentFocus`. — installed via `adb -s emulator-5554 install -r`, launched via `am start -n com.eight87.whisperboy/.WhisperboyActivity`; `dumpsys window | grep mCurrentFocus` returns `Window{... com.eight87.whisperboy/com.eight87.whisperboy.WhisperboyActivity}`. Screencap at `/tmp/whisperboy-A6.png` shows the template's "Hello Android!" Compose surface rendering correctly.

**Shipped:** A.0–A.6 in commit _(this commit)_.

---

## Phase B — core playback

Goal: ExoPlayer plays a known audio file. `MediaLibrarySession` is registered. Audio focus is honored. **Voice analog:** `:core:playback` (`PlaybackService`, `VoicePlayer`, `LibrarySessionCallback`).

- [x] **B.1** `PlayerHolder` wraps an `ExoPlayer` instance. Build with `setAudioAttributes(..., handleAudioFocus = true)`, `setHandleAudioBecomingNoisy(true)`, `setWakeMode(WAKE_MODE_LOCAL)`. Use a **`OnlyAudioRenderersFactory`** (audio-only `RenderersFactory` — strips video renderers entirely; lifted as a *pattern* from Voice, not vendored) to keep APK small and avoid any video decoder surface. — `app/.../playback/PlayerHolder.kt` + `OnlyAudioRenderersFactory.kt`. Audio attrs use `USAGE_MEDIA` + `CONTENT_TYPE_SPEECH` (audiobook-correct; routes voice content through the system's voice pipeline). `OnlyAudioRenderersFactory` extends `DefaultRenderersFactory` and overrides `buildVideoRenderers` to a no-op. Composition-root scaffolding (`AppGraph`, `WhisperboyApplication`) ships alongside since the Player is a long-lived singleton.
- [x] **B.2** `PlaybackService : MediaLibraryService` declared in the manifest with `foregroundServiceType="mediaPlayback"` and the `androidx.media3.session.MediaLibraryService` intent filter. Stub notification (replaced for real in Phase F). — `PlaybackService.kt` extends `MediaLibraryService`. Manifest entry declares both `MediaLibraryService` and `MediaSessionService` action filters because Media3's client discovery varies by surface (Auto/Wear-OS browse goes via the library service; direct controllers can land on either). Default Media3 notification is sufficient as the stub; Phase F replaces it with `MediaStyle`.
- [x] **B.3** `MediaLibrarySession` wired to the Player via `MediaLibrarySession.Builder(this, player, callback)`. Stub `MediaLibrarySession.Callback` returns an empty browse tree — real tree lands in Phase N. — `WhisperboyLibrarySessionCallback` is an empty class implementing the Media3 1.10.0 `MediaLibrarySession.Callback` interface; default impls return `RESULT_ERROR_NOT_SUPPORTED` for the library-tree methods, which is correct for "no browse tree yet". `PlaybackService.onCreate` builds the session from `(application as WhisperboyApplication).graph.playerHolder.player`. `onTaskRemoved` stops the service when the user swipes the app away while paused (mirrors Voice's pattern).
- [x] **B.4** Audio focus delegated to ExoPlayer's built-in handling via `setAudioAttributes(..., handleAudioFocus = true)`. (Same pattern tonearmboy validated; verified against `kb://android/media/media3/session/background-playback`.) — covered by the `PlayerHolder` config in B.1; no separate `AudioFocusRequest` needed.
- [x] **B.5** Format smoke test on the real target: play one each of MP3, M4B (with embedded chapters), OGG Vorbis, FLAC, WebM/Matroska. Tonearmboy's smoke pattern (`scripts/smoke-test.sh`, broadcast intent → service plays via `/data/local/tmp` fixtures) ports cleanly to whisperboy. The M4B + Matroska format coverage is new; both pass at the codec layer (Phase I parses the chapter markers — Phase B just needs the audio to decode). — `scripts/smoke-test.sh` generates 1-second sine fixtures with `ffmpeg`, pushes to `/data/local/tmp/whisperboy-smoke/`, broadcasts `com.eight87.whisperboy.action.SMOKE_PLAY` with each path → `SmokePlayReceiver` hands the path to the player and logs `Player.STATE_*` transitions under the `whisperboy.smoke` tag → script asserts `STATE_READY` for each format. **All five pass on `emulator-5554` (API 36):** `mp3 ok / m4b ok / ogg ok / flac ok / webm ok`. M4B exercises the MP4 sniffer with the `.m4b` extension (same audio bytes as `.m4a`); Phase I will add proper M4B chapter-atom parsing.

**Shipped:** B.1–B.5 in commit _(this commit)_.

---

## Phase C — SAF folder picker + persisted URI permissions

Goal: the user can pick one or more folders, whisperboy persists `Uri.persistableUriPermission` across reboots, and the picked tree is reachable as a `DocumentFile`. **Voice analog:** `:features:folderPicker`, `PersistedUriPermissions`.

- [x] **C.1** Onboarding entry — first launch shows a "Pick your audiobook folder" empty state with a single CTA. (Real onboarding flow lands in Phase L; this is the placeholder bridge.) — `HomeScreen.kt` (rewritten — template ballast deleted, see R.F.1) gates on `roots.isEmpty()`. Empty state: title `folder_picker_empty_title` + subtitle `folder_picker_empty_subtitle` + CTA button `folder_picker_cta`. Verified on `emulator-5554` — screencap at `/tmp/whisperboy-C-empty.png` shows the empty state rendering correctly.
- [x] **C.2** `Intent.ACTION_OPEN_DOCUMENT_TREE` launched via `ActivityResultContracts.OpenDocumentTree`. On success, take persistable read permission via `contentResolver.takePersistableUriPermission(uri, FLAG_GRANT_READ_URI_PERMISSION)`. — `rememberLauncherForActivityResult(ActivityResultContracts.OpenDocumentTree())` in `HomeScreen.kt`; on result, the URI is held in local Compose state and the `FolderTypeSheet` opens. `AndroidPersistedUriPermissionStore.addRoot` calls `takePersistableUriPermission` with `FLAG_GRANT_READ_URI_PERMISSION` on `Dispatchers.IO`. Verified end-to-end: `dumpsys window` confirms `com.android.documentsui.picker.PickActivity` in focus after the CTA tap; back-press returns to whisperboy without crash.
- [x] **C.3** `PersistedUriPermissions` repo — list, add, remove. Backed by `contentResolver.persistedUriPermissions`. Surfaces a `Flow<List<Uri>>` of currently-held trees. — `data/library/PersistedUriPermissionStore.kt` (interface) + `AndroidPersistedUriPermissionStore.kt` (impl). Backed by **DataStore Preferences** for the `(treeUri → FolderType, displayName)` mapping (since the FolderType isn't stored in `ContentResolver.persistedUriPermissions`). Exposes `Flow<List<LibraryRoot>>` instead of bare `Flow<List<Uri>>` so consumers always get the FolderType together with the URI. AppGraph exposes only the interface — composables never see the Android impl (R.A pattern, ticked separately).
- [x] **C.4** `FolderType` sealed type with the four Voice variants (`SingleFile`, `SingleFolder`, `Root`, `Author`). Each picked tree gets a `FolderType` (default = `Root`, picker shows the four options as a follow-up bottom sheet on add). — `data/library/FolderType.kt` is a sealed interface with the four `object` variants + companion `id(type)` / `fromId(id)` for stable persistence and `allOrdered` for UI iteration. `HomeScreen.kt`'s `FolderTypeSheet` is a Material 3 `ModalBottomSheet` listing the four options with title + subtitle from `folder_type_<variant>_{title,subtitle}` strings.
- [x] **C.5** `CachedDocumentFile` wrapper around `DocumentFile` (caches `name` / `length` / `lastModified` / `isDirectory` / `listFiles` results in-process — SAF round-trips are otherwise miserable on large trees, especially on SD card). **Voice analog:** `:core:documentfile`. — `data/library/CachedDocumentFile.kt` wraps `androidx.documentfile.provider.DocumentFile` with `by lazy` caching of `name`, `length`, `lastModified`, `isDirectory`, `isFile`, `type`, and `children` (recursive). Per-instance cache, not global — Phase D's scanner constructs fresh instances on rescan.
- [x] **C.6** Settings → Library page lists current roots with their `FolderType` + remove button (revokes the persistable permission). Real Settings UX lands in Phase K; this is the minimum to unblock multi-root testing in Phase D. — Currently lives **inside** `HomeScreen.kt` as the configured-state branch (when `roots.isNotEmpty()`): `LazyColumn` of `LibraryRootRow`s showing the display name + FolderType label + Remove button. Tapping Remove calls `persistedUriPermissionStore.removeRoot(uri)` which releases the permission via `releasePersistableUriPermission`. Phase K (`SettingsScreen` → Library section) will move this to a proper settings sub-page; the current bridge is the minimum to unblock Phase D.

**Shipped:** C.1–C.6 in commit _(this commit)_. Also ticks **R.F.1** (template ballast deleted: `data/DataRepository.kt`, `ui/home/HomeScreenViewModel.kt`, the `home_*` strings, the placeholder Greeting composable — all gone since `HomeScreen.kt` now renders the actual library bridge) and **R.A pattern** (composables take only `PersistedUriPermissionStore`, never the concrete impl or `AppGraph`).

---

## Phase D — scanner: walk SAF tree, classify, persist — Status: ✅ DONE

Goal: walk every root, classify folders by `FolderType`, materialize Book / Chapter rows in Room, drop dropped books to `inactive` (soft-delete) on rescan. **Voice analog:** `:core:scanner` (`MediaScanner`, `BookParser`, `ChapterParser`, `MediaAnalyzer`).

- [x] **D.1** Room schema baseline: `BookEntity` (book id = SHA-256 of root tree URI + relative path; speed, skipSilence, gain, name, author, cover, currentChapterIndex, positionInChapter, lastPlayedAt, active), `ChapterEntity` (book id, index, name, duration, file uri or position-within-file, list of `ChapterMark`s), `BookmarkEntity` (uuid, book id, chapter id, title, time, addedAt, setBySleepTimer). Schema export on; Room version 1; no `fallbackToDestructiveMigration`. — three per-entity files in `data/library/` (R.F.2 ticks): `BookEntity.kt`, `ChapterEntity.kt`, `BookmarkEntity.kt` with FK constraints (`Chapter` → `Book` CASCADE, `Bookmark` → `Book` CASCADE, `Bookmark` → `Chapter` SET_NULL). DAOs land in `BookDao.kt` / `ChapterDao.kt` / `BookmarkDao.kt`, all suspend except observe-Flows (R.F.3 ticks for the data layer). `LibraryDatabase.kt` is `@Database(version = 1, exportSchema = true)`; **no `fallbackToDestructiveMigration`** — any v1→v2+ change must ship a migration. Schema JSON written to `app/schemas/com.eight87.whisperboy.data.library.LibraryDatabase/1.json` by Room's gradle plugin (`room { schemaDirectory("$projectDir/schemas") }` in `app/build.gradle.kts`). `ChapterMark` deferred to Phase I (M4B / Matroska parsing); for now chapters are scalar rows. Database constructed in `AppGraph` via `Room.databaseBuilder(... "library.db")` so a misconfigured schema fails fast at app start, not on first scan. Narrow data interfaces shipped alongside (R.A.1 partial tick — see `refactor-solid.md`): `BookSource`, `ChapterSource`, `BookmarkSource`. Concrete repository lands in D.4.
- [x] **D.2** `Scanner` walks each root via `CachedDocumentFile`, dispatches by `FolderType`:
  - `SingleFile` → one `Book`, chapters from embedded markers (Phase I) or one chapter == whole file
  - `SingleFolder` → one `Book`, chapters = audio files in alphabetical order (`BookComparator`)
  - `Root` → each first-level subfolder is a separate `Book`
  - `Author` → first-level subfolders are authors, second-level are `Book`s

  Shipped as `data/library/SafLibraryScanner.kt` (impl) behind `LibraryScanner` interface (R.A.1 — narrow data interface). Dispatch is an exhaustive `when (root.folderType)` over the sealed `FolderType` (R.A pattern: O — open/closed; new variants land as new cases, no `if` chains). Scan is a `suspend` function that runs on `Dispatchers.IO` end-to-end (R.F.3); no `runBlocking` on the scan path. Stable identifiers via SHA-256: `bookIdFor(treeUri, relativePath)` and `chapterIdFor(bookId, index)` — same input → same id across rescans, so per-book settings + bookmarks survive. Audio file detection via `SupportedAudioFormats` (mp3 / m4a / m4b / aac / 3gp / ogg / oga / opus / flac / wav / mka / mkv / webm / mp4 — covers Voice's set and the Phase B.5 codec smoke). Output is `ScanSnapshot(books)` with `ScannedBook` / `ScannedChapter` types **separate** from the Room entities (R.F.4 — scan-side fields like raw cover bytes can land here without polluting cache schema). Phase D.2 fills structural fields only; `durationMs` / metadata-`title` / `author` / `cover` / `positionInBookMs` stay at defaults until Phase D.3's `MediaAnalyzer` enriches and Phase D.4's `applyScan` computes cumulative offsets. Wired into `AppGraph.libraryScanner` as the narrow interface; concrete impl is internal-able when D.4 lands. Unit tests: `SafLibraryScannerTest` covers ID-stability across calls, ID divergence across treeUri / path / chapter index, and that `bookIdFor` returns 64-char lowercase hex SHA-256. Walking-the-actual-tree behaviour will be covered by D.6's smoke test against a real fixture folder on `emulator-5554`.
- [x] **D.3** `MediaAnalyzer` extracts duration + title/author/cover via `MediaMetadataRetriever` for each audio file. Cover bytes: prefer embedded → fallback to `cover.jpg` / `folder.jpg` next to the audio file. — shipped as four files in `data/library/`: `MediaAnalyzer.kt` (narrow interface — R.A.1 extension — plus `FileMetadata` data class with custom `equals`/`hashCode` for the embedded cover `ByteArray`), `Media3MediaAnalyzer.kt` (impl using `android.media.MediaMetadataRetriever`; reads `METADATA_KEY_DURATION` / `_TITLE` / `_ARTIST` (fallback `_ALBUMARTIST`) / `embeddedPicture`; suspend on `Dispatchers.IO`; bad files don't crash the scan — `Throwable` caught, returns `null`, retriever released in `finally`), `FolderCoverFinder.kt` (small helper checking `cover.{jpg,jpeg,png,webp}` / `folder.{jpg,jpeg,png,webp}` / `albumart.{jpg,png}` next to the audio, case-insensitive), and `LibraryScannerEnrichment.kt` (glue: walks `ScanSnapshot.books`, calls `MediaAnalyzer.extract` per chapter, fills `durationMs` / `title`, computes cumulative `positionInBookMs` across the chapter list in one pass, rolls book-level fields up — `durationMs` = sum of chapter durations, `author` = first non-null per-chapter author). Cover-bytes-to-disk write is deferred to Phase D.4's `applyScan` so the file lifecycle stays bound to the row write transaction. Wired into `AppGraph.mediaAnalyzer` and `AppGraph.libraryScannerEnrichment` as narrow handles. Suspend throughout, no `runBlocking` (R.F.3 still holds). Build + `:app:testDebugUnitTest` clean.
- [x] **D.4** Diff & apply: scanner produces a `ScanSnapshot`; `LibraryRepository.applyScan(snapshot)` writes only changed rows in a single Room transaction, marks gone-but-was-here books `active = false` (soft-delete preserves bookmarks + position if the user re-adds the folder). — shipped as four new files in `data/library/`: `ScanWriter.kt` (one-method narrow interface — R.A.1 extension — separating the write path from the three read sources), `CoverStore.kt` (atomic `<filesDir>/covers/<bookId>` writer; tmp-then-rename, fallback copy-then-delete on rename failure), `LibraryRepository.kt` (`internal class` implementing all four narrow interfaces — R.A.2 ticks). Plus extensions: `BookmarkDao.rename(id, title)` for the rename path; `ScannedBook.embeddedCoverBytes` for D.3 → D.4 cover-byte propagation, with custom `equals`/`hashCode` on the `ByteArray` field; `LibraryScannerEnrichment` propagates first-non-null embedded cover from per-chapter metadata. **applyScan algorithm:** (1) write any cover bytes to disk *outside* the Room transaction (long file IO inside a txn would hold the database lock; rollback would orphan the file); (2) gather the set of `treeUriString`s touched by the incoming snapshot; (3) `database.withTransaction { … }` runs: mark every active book from each touched root inactive (`bookDao.markRootInactive(treeUri)`), then upsert incoming books (which sets `active = true` on the upsert), then for each book delete-and-replace its chapter rows (CASCADE on chapter → bookmark FK is `SET_NULL` so bookmarks survive a chapter row disappearing — they unpin from the chapter and stay pinned to the book-level position). Result: missing books soft-deleted with bookmarks + per-book settings preserved; added/changed books reflect reality; bookmarks survive chapter reshuffles. AppGraph now exposes `bookSource` / `chapterSource` / `bookmarkSource` / `scanWriter` as narrow handles; the concrete `LibraryRepository` is held privately, never leaked. Per-book settings (resume position / speed / skipSilence / gain / lastPlayedAt) are reset to defaults on rescan in this phase — preserving them across rescans is a tracked follow-up that lands when Phase F's playback wires in (it needs the merge-with-existing-row code path). FolderCoverFinder integration (cover.jpg fallback) is also tracked as a follow-up — D.3's enrichment doesn't have folder-context yet, so the helper is wired into AppGraph but not called. Build + `:app:testDebugUnitTest` clean.
- [x] **D.5** Rescan triggers: manual (Settings → Rescan), on app foreground (debounced 30s), on persistable URI permission added/removed. **No `ContentObserver`** — SAF doesn't surface change events the same way `MediaStore` does; we rescan on signal, not on observation. — shipped as `data/library/LibraryRescanCoordinator.kt` (narrow interface, R.A.1 extension: `requestRescan()` + `state: StateFlow<RescanState>`) and `AndroidLibraryRescanCoordinator.kt` (impl). All three trigger paths drain into one **conflated channel** of rescan signals — at most one scan runs, at most one queued; subsequent triggers while one is queued are dropped, but each scan picks up the **latest** root set via `observeRoots().first()` so a root added between trigger and pickup still gets scanned. Trigger paths: (1) **manual** — `requestRescan()` is the public method; HomeScreen's "Rescan now" button + Phase K's eventual Settings → Rescan call this; (2) **foreground** — registers a `DefaultLifecycleObserver` on `ProcessLifecycleOwner.get().lifecycle`; `onStart` fires `requestRescan` debounced 30s (`foregroundDebounceMs` configurable, defaults to `30_000L`); (3) **root-set-change** — collects `persistedUriPermissionStore.observeRoots()` mapped to set of `treeUri`s, runs through `distinctUntilChanged`, fires `requestRescan` on each change (initial emission also fires — that's the cold-start scan). `RescanState` sealed (`Idle` / `Running` / `Failed(cause: Throwable)`) so the UI can render a "tap to retry" affordance with diagnostic context. AppGraph adds an application-scoped `CoroutineScope(SupervisorJob() + Dispatchers.IO)` used by the coordinator for its long-lived collectors. HomeScreen's configured-state branch gains a "Rescan now" `OutlinedButton` (disabled while `state is Running`) plus a small `RescanStateBadge` inline-text that shows `"Scanning your library…"` / `"Scan failed — tap to retry"` colored to the theme's `primary` / `error`. Strings: `folder_rescan_action`, `folder_rescan_state_running`, `folder_rescan_state_failed`. Build dep added: `androidx.lifecycle:lifecycle-process` (already on the lifecycle 2.10.0 family). Suspend throughout; R.F.3 holds. Build + `:app:testDebugUnitTest` clean; install + launch on `emulator-5554` clean (no FATAL in logcat). Real scanning behaviour gets exercised end-to-end by D.6's smoke script.
- [x] **D.6** `library-smoke-test.sh` — fetches two synthetic test books (one `SingleFolder` 3-chapter book, one `SingleFile` M4B with embedded chapters), pushes to `/sdcard/Audiobooks/whisperboy-test/`, points the SAF picker at it via the canonical onboarding flow, asserts both books land in Room with the right chapter counts. — shipped as `scripts/library-smoke-test.sh` end-to-end against `emulator-5554` (API 36, `com.google.android.documentsui` picker). Exercises every previous Phase D inch in one run: ffmpeg generates a SingleFolder-shaped book (3 mp3 chapters at 1s each, distinct frequencies for sanity) + a SingleFile-shaped book (single 2s m4b — embedded chapter parsing is deferred to Phase I per the plan), pushes both into `/sdcard/Audiobooks/whisperboy-test/`, `pm clear`s app data so the cold-start scan path runs from zero, installs the latest debug APK, drives the SAF picker programmatically via `uiautomator dump` + a small `tap_text` helper (find node bounds by `text="…"` substring, `input tap` the center) — taps "Pick an audiobook folder", navigates `Audiobooks → whisperboy-test`, taps "USE THIS FOLDER" / "ALLOW", picks "Root" in the FolderType bottom sheet — then polls logcat for the `whisperboy.scan` SMOKE_TAG line emitted by `AndroidLibraryRescanCoordinator.runScan` (D.6 instrumentation: `Log.i(SMOKE_TAG, "SCAN_COMPLETE roots=N books=M chapters=K")` after the scan; `Log.e(SMOKE_TAG, "SCAN_FAILED …")` on the catch). Asserts `roots=1 books=2 chapters=4` from the post-picker scan (filter `roots>=1` so the cold-start `roots=0` scan is skipped). 3-for-3 PASS in stability run; idempotent on picker state (force-stops `documentsui` between runs); cleanup on `EXIT` removes the remote fixtures. SAF persistable URI permissions can ONLY be granted by the system picker (no `pm grant` shortcut), so the picker walk *is* the test — that's why the script bothers driving uiautomator instead of synthesizing a tree URI directly.

---

## Phase E — book overview UI (the library screen)

Goal: cover-grid library screen, three filter chips (Current / Not started / Completed), sort menu, search bar, long-press → bottom sheet (rename, change cover, mark completed, delete book record). **Voice analog:** `:features:bookOverview`.

> **Read [`cold-start-perf.md`](cold-start-perf.md) before starting.** Phase E is the first surface that mounts a `LazyVerticalGrid` + filter Flows + sort-menu state — exactly the shape tonearmboy's perf session had to claw back. Specifically: `collectAsStateWithLifecycle` (B.1) for the book Flow, stable `key = { it.id }` (E.1) on the grid `items(...)`, `contentType = { ... }` (E.2) when E.1 lands a list-toggle alongside the grid, `Modifier.graphicsLayer { alpha = ... }` (C.2) for the cover progress overlay if it animates. Also: R.F.1 (template-ballast purge) ticks here, per `refactor-solid.md`.

> **Read [`m3-expressive.md`](m3-expressive.md) before starting.** Phase E is the first surface that mounts visible chrome, so the M3E foundation lands here too: bump `material3` to `1.5.0-alpha18` (gotcha #1: stable 1.4.0 keeps the expressive APIs `internal`), wrap the theme entry in `MaterialExpressiveTheme(...)` (m3e A.3), switch to `expressiveDarkColorScheme()` (m3e A.4 — note `expressiveLightColorScheme()` only; no dark factory in alpha18), audit page bg vs card bg (m3e B.1 — `surface` vs `surfaceContainerHigh` on AMOLED dark). The library grid itself is m3e D.1.

> **Read [`cover-art.md`](cover-art.md) before starting.** Phase E mounts the first `CoverArt` consumer (the library grid). The plan mirrors **Voice's `features/cover/` module** almost exactly: local-first scan (existing → SAF directory first `image/*` → embedded from first 5 chapters), user-initiated DuckDuckGo image search per-book only, crop overlay after pick. Phase A of that plan lands here (local-first scan + placeholder glyph + decode-size discipline — **never `Size.ORIGINAL` for tiles**, tonearmboy `8d8c1a4` lesson). The Search-online entry point is the long-press sheet's "Search online" action (Phase B of cover-art.md). **No cover-art settings exist** — Voice doesn't expose any, neither do we.

- [x] **E.1** `LibraryScreen` composable — cover-forward grid, default columns auto-sized to width, list-toggle in the top app bar (`GridMode` sealed type). Each cover renders the book title underneath + a thin progress bar overlay along the bottom edge of the cover (% of total duration listened). **`FastScrollbar`** (per `tonearmboy/ui/common/FastScrollbar.kt` — commits `52d0b81` → `4e2ff73`) wraps the grid: section-letter chips along the right edge at each section's fractional Y, visible during `isScrollInProgress || isDragging`, 800ms linger before fade-out. Top-app-bar `expandedHeight = 32dp` (the resting value tonearmboy converged to across 11 screens — `2da7c07` → `3903d71` → `088630b`). — **shipped:** `LibraryScreen` lands as the entry surface when roots are configured (HomeScreen hands off); `GridMode.Grid` / `GridMode.List` toggle in the top app bar. `FastScrollbar` ported whole-file from tonearmboy (MIT→MIT, same author) into `ui/common/`; supports both `LazyListState` and `LazyGridState`. Section-letter chips wired against a `titleSectionStarts(books)` helper — sort-aware variants land at E.3. Top-app-bar `expandedHeight = 32.dp`, surface-tier `surfaceContainer`. Placeholder `CoverArt` (book glyph in `surfaceContainerHigh`) — Coil + real cover loading lands at cover-art.md Phase A. Folder-management affordances (Rescan / Add folder / Manage folders) live in the top app bar overflow as an interim home until Phase K's settings tree lands. Progress bar overlay deferred to follow-up — needs `BookEntity.positionInChapterMs` × `currentChapterIndex` math against chapter durations, gates on cover-art Phase A shipping the cover Coil hookup so the overlay can ride the same composable. Smoke verified: `library-smoke-test.sh` PASS roots=1 books=2 chapters=4; manual 26-book scroll confirms FastScrollbar thumb + section chips visible during scroll.
- [x] **E.2** Filter chips row: Current / Not started / Completed. Filter is a sealed `BookFilter`; query plumbed through `BookSource.observeBooks(filter)`. — **shipped (partial — Completed deferred to E.5):** `BookFilter { All, Current, NotStarted }` enum in `ui/library/LibrarySorting.kt`; pure-function `filterBooks` predicate. `Completed` deliberately absent until E.5's "Mark completed" action lands — without an explicit user-set flag, "completed" can only be inferred from position math (`currentChapterIndex` × `positionInChapterMs` vs `durationMs`) which is unreliable before the user has actually played to the end. Voice ships an explicit flag; we will too when E.5 lands the schema migration (`completedAt: Long?` column on `BookEntity`). Chip row uses M3 `FilterChip` between top app bar and grid; chained pipeline `books → filterBooks → sortedBooks → sectionStartsFor` re-derives on each user change via `remember(...)`. Filter applied IN-MEMORY rather than via a `BookSource.observeBooks(filter)` overload — keeps the Room query simple, defers the SQL filter to whenever library size makes the in-memory pass actually expensive (likely never for typical user collections; tonearmboy didn't push this through to DAO either). Tests: 3 additional in `LibrarySortingTest`. **Chip → rail upgrade (commit `7834c63`):** the horizontal `FilterChip` row was replaced with a hand-rolled 52 dp vertical tab rail (`ui/library/LibraryRail.kt`) mirroring tonearmboy's pattern — rotated text labels, bold + 2 dp primary accent stripe for the active filter, `verticalScroll` for overflow. Rail hides in search + empty-library states; the now-playing bar stays full-width as a sibling below the rail/content `Row`.
- [x] **E.3** Sort menu (Recent, Title, Author). Persisted in DataStore. **Sort indicator** in the top app bar shows the active sort key (tonearmboy `bd525f9` + `d75b542` — initial circle indicator dropped). **Sort-aware section keys**: when sorting by Title, the FastScrollbar `sectionStarts` is the alphabet; when sorting by Recent, it's date buckets; when sorting by Author, the author letter. **Duplicate-key crash gotcha** (`d75b542`): `LazyVerticalGrid` keys must be unique across the rendered set — when a "section header" item type sits in the same grid as book tiles, the header's key must include a discriminator (`"section:$letter"`), not just the letter, otherwise two ranges with the same letter blow up. — **shipped (in-memory persistence pending):** `BookSortKey { Recent, Title, Author }` + pure-function `sortedBooks` and `sectionStartsFor` in `ui/library/LibrarySorting.kt`. Title → alphabet chips (`#` bucket for non-letter starts); Author → first-letter chips (`?` bucket for unknown-author); Recent → `Today` / `Week` / `Month` / `Older` / `Never` buckets relative to `nowMs`. Top app bar gains a Sort icon button with a `DropdownMenu`; current selection has a check leading icon (no circle indicator — matches `d75b542` final shape). Section chips re-derive on every sort change via `remember(sortedBooks, sortKey)`. `LibrarySortingTest` covers all three sort modes + all three section-start label modes (7 tests, JVM-only). **Follow-up shipped:** `data/library/LibraryUiSettings.kt` — narrow facet (three `Flow<X>` properties + three `suspend setX(...)` setters, R.A pattern) backed by `AndroidLibraryUiSettings` over a `library_ui` DataStore-Preferences file in `AppGraph`. Persists `gridMode`, `sortKey`, and `filter` across app restarts. The three enums moved to `data/library/LibraryUiPrefs.kt` so the facet doesn't invert the dependency direction (data → ui); the pure-function predicates / comparators stay in `ui/library/LibrarySorting.kt`. `LibraryScreen` reads via `collectAsStateWithLifecycle` (cold-start-perf.md B.1) and writes via `coroutineScope.launch { settings.setX(...) }`. Unknown / null preference values coerce to defaults (`Grid` / `Title` / `All`). Test coverage in `AndroidLibraryUiSettingsTest` (8 cases — defaults, round-trip per enum value, unknown-name fallback, cross-instance persistence).
- [x] **E.4** Search — full-text over book title + author. Room FTS4 if it composes cleanly with the `BookEntity` shape, else `LIKE` fallback. Same pattern tonearmboy validated in C.4. — **shipped (in-memory; LIKE/FTS deferred):** pure-function `searchBooks(books, query)` in `ui/library/LibrarySorting.kt`; case-insensitive substring over title + author with whitespace-trimmed query (blank/empty returns input unchanged). Search bar swaps into the top app bar position when the search-icon action is tapped (`searchMode = true`); close icon + clearing query restores the normal app bar. Filter chips hide while in search mode (chained pipeline becomes `books → filter → search → sort → sectionStarts`). No-results state shows `library_search_no_results` with the query echoed back. In-memory search is fine for typical libraries; same call tonearmboy made — DB-side FTS4 (`BookDao.search` already has a LIKE-based variant; FTS4 entity migration deferred) becomes meaningful only at 1000+ book scale. Tests: 4 additional in `LibrarySortingTest`. UI wiring deferred a worktree's-touch from a parallel Phase F skeleton subagent.
- [x] **E.5** Long-press → `BookActionSheet` (Rename, **Use custom cover from device** / **Search online for cover** — per [`cover-art.md`](cover-art.md), Mark completed, Mark not started, Forget book record). "Forget" only deletes the row + bookmarks — does not delete files (whisperboy doesn't delete audiobook files; the user's library is precious). **Multi-select**: long-press on a tile flips into selection mode (state hoisted via `rememberSelectionState()` — `refactor-solid.md` R.D.2, validated in tonearmboy `40da803`); subsequent taps toggle; back press clears. `MultiSelectBar` at the top renders the count + close + "Mark completed" / "Forget" bulk actions. Selection background uses `secondaryContainer` painted by the row wrapper, not per-row (so list + grid both inherit the highlight without re-implementing). — **shipped (partial — multi-select + Rename + cover actions deferred):** schema migration v1 → v2 adds `completedAt: Long?` column on `BookEntity` with explicit `MIGRATION_1_2` registered in AppGraph (no destructive fallback). DAO gains `setCompletedAt` + `setLastPlayedAt`; `BookSource` gains `markCompleted` / `markNotStarted` / `forgetBook`. `BookFilter.Completed` chip lands as the fourth filter; predicate excludes completed books from `Current` + `NotStarted` (E.5 unlocks E.2's deferred chip). Tile + row use `combinedClickable` for long-press → `BookActionSheet` `ModalBottomSheet` with Mark completed / Mark not started / Forget book (the Mark variant shown depends on the book's current `completedAt` state). Forget kicks an `AlertDialog` confirm before delete; on confirm calls `bookSource.forgetBook(bookId)` which deletes the row + cover (FK CASCADE handles chapters / bookmarks). 4 new `LibrarySortingTest` cases (NotStarted-excludes-completed, Current-excludes-completed, Completed-keeps-non-null, ...). **Deferred to follow-ups**: Rename action (needs an inline title editor — Voice has one, defer), Use custom cover from device + Search online for cover (cover-art.md A.6 + B), multi-select via `rememberSelectionState()` + `MultiSelectBar` (R.D.2 — needs the bulk-action surface; defer).
- [x] **E.6** Now-playing bar pinned to the bottom when something is playing — book title, chapter title, play/pause, taps through to the player screen (Phase F). **Auxio-style thin progress line** (2dp, tonearmboy `82d6248`) along the bottom edge of the bar reads the same `NowPlayingState.position / duration` fraction the player scrubber uses. **Lazy-mount behind sheet** (tonearmboy `3436ae2`): the full now-playing surface mounts only when the user expands the sheet; collapsed state renders only the slim bar — keeps cold-start light. — **shipped:** `ui/library/NowPlayingBar.kt`. Self-hides via `state as? PlaybackUiState.Loaded ?: return` — no slot reserved when nothing playing (effectively the lazy-mount discipline; the full player is a separate route, not an expanded sheet — there's no heavy composable to gate). Layout: 48dp cover thumbnail + title/chapter stacked + play/pause icon button + 2dp progress line spanning the bottom (`surfaceContainerLowest` track + `primary` fill). Progress line uses `Modifier.graphicsLayer { scaleX = fraction; transformOrigin = (0f, 0f) }` (cold-start-perf C.2) so the fast-ticking position read defers to draw — no recomposition on each 250ms position tick. Tapping the bar fires `onTap(bookId)` (same callback the tile tap uses) → pushes `PlaybackRoute(bookId)`. Play/pause toggles transport without leaving the library — Voice's pattern. **Sheet refactor follow-up (shipped in change `030b0b7`):** the bar is no longer mounted inline in `LibraryScreen`; it lives at the `WhisperboyApp` root inside `ui/playback/NowPlayingSheet.kt` as the **peek** of an Auxio-style overlay sheet (modeled on tonearmboy `TonearmboyApp`). The bar's outer Row gains a `detectVerticalDragGestures` pointerInput that forwards drag deltas to the host's shared `Animatable<Float>` (`sheetProgress`); the `onTap(bookId)` callback became `onExpand()` (the host already knows which book is loaded). Library no longer takes `nowPlayingState` / `transportCommands` params — those flow directly into the sheet at the root.

---

## Phase F — playback screen (now-playing)

Goal: full-screen player. **Voice analog:** `:features:playbackScreen`.

> **Read [`cold-start-perf.md`](cold-start-perf.md) before starting.** The player is the highest-frequency-recomposition surface in the app — the position ticker (F.2) fires every ~250ms while playing. Critical guards: `collectAsStateWithLifecycle` (B.1) on `PlaybackUiState` so collection defers to `Lifecycle.STARTED`; `Modifier.offset { IntOffset(...) }` (C.1) for the scrubber thumb so the State read happens in layout, not composition; `Modifier.graphicsLayer { alpha = ... }` (C.2) for the Palette-tinted background gradient (F.6) so its animated alpha doesn't trigger recomposition each frame; do NOT use `BoxWithConstraints` (B.3) for the cover-square sizing — read `LocalConfiguration.current` instead.

- [x] **F.1** `PlaybackScreen` composable — large centered square cover dominates the top half; book title + chapter title stacked below; scrubber with current-position / chapter-duration timestamps; transport row (prev-chapter, rewind-N-seconds, play/pause, forward-N-seconds, next-chapter); top app bar with back, sleep-timer button, bookmark button, overflow. — **shipped:** `ui/playback/PlaybackScreen.kt` renders four state branches (`Idle` / `Loading` → spinner; `BookNotFound` → error; `Loaded` → full chrome). Takes narrow `NowPlayingState` + `TransportCommands` only (R.A discipline — no `BookSource` / `PlaybackController` import). `collectAsStateWithLifecycle` (cold-start-perf B.1) for state read; `LocalConfiguration.current` sizing path planned but cover currently sizes by `fillMaxWidth(0.85f) + aspectRatio(1f)` (good enough; no `BoxWithConstraints` per B.3). Top-app-bar at `expandedHeight = 32.dp` per m3-expressive gotcha #6. Sleep timer / bookmark / overflow buttons present as no-op stubs (Phase G / H). Rewind / forward hard-coded 30s — configurable is F.3. **Sheet refactor follow-up (shipped in change `030b0b7`):** `PlaybackRoute` retired from the user-driven nav path — `PlaybackScreen` is now mounted by `ui/playback/NowPlayingSheet.kt` (Auxio-style overlay at the `WhisperboyApp` root), lazy-mounted behind `sheetProgress > 0.45f` so the heavy player chrome (cover load, scrubber, transport row, lifecycle Flows) defers off cold start. `onBack` is now the sheet's `closeSheet` (animate to 0f) rather than a back-stack pop. `PlaybackRoute` retained in `NavigationKeys.kt` for future deeplinks (notification tap / Auto launch) which will animate the sheet open rather than push a route. Optional `chapterListState: LazyListState?` stub param added for future host-driven nested-scroll → sheet-collapse wiring (currently unused — chapter list lives in `ChapterListSheet` ModalBottomSheet, not directly in the player body).
- [x] **F.2** `PlaybackController` (UI-side wrapper around `MediaController`) exposes `StateFlow<PlaybackUiState>` (book, chapter, position, playing, speed, skipSilence, gain). UI consumes via `collectAsStateWithLifecycle`. Position ticker runs at ~250ms while playing, off when paused. — **shipped:** `playback/PlaybackController.kt` (`internal class`) implements four narrow interfaces (`NowPlayingState` + `TransportCommands` + `BookCommands` + empty stub `SleepTimerCommands`) — R.A.2 + R.C.1. AppGraph exposes each interface separately; module-external code never sees the concrete class. Connection via `SessionToken` + `MediaController.Builder(...).buildAsync()` against the existing `PlaybackService`. Listener-driven snapshot (`PlayerSnapshot`) tracks `isPlaying / mediaId / itemIndex / speed` on `Player.Listener` events; **position ticker is a SEPARATE coroutine** at 250ms while `isPlaying`, idle on pause (R.C.5 — a position tick does NOT recompute the queue). `state` projection composes `connected ⊗ targetedBookId` → either `Loading` / `Idle` / `Loaded`. Skip-silence + gain stubs land their request into `PlayerSnapshot` (cross-process `MediaController` doesn't expose those knobs directly — wired through in Phase J). Nav glue: `WhisperboyApp` adds an `entry<PlaybackRoute(bookId)>` block; `LibraryScreen` tile clicks call `onBookTap(bookId)` which pushes the route; `LaunchedEffect(bookId) { bookCommands.playBook(bookId) }` kicks playback on route landing.

**Shipped:** F.1, F.2 in commits `9780309` (controller + state + nav key + strings — landed alongside E.4 due to a worktree-isolation leak from the parallel subagent dispatch; the subagent wrote to absolute paths so its files ended up in the main tree before its own commit) + this commit (PlaybackScreen + AppGraph wiring + WhisperboyApp entry + LibraryScreen book-tap glue + FolderCoverFinder cleanup). F.3 (configurable rewind/forward seconds), F.4 (auto-rewind on resume), F.5 (chapter list bottom sheet), F.6 (Palette-tinted background), F.7 (notification + lock-screen — basic `MediaStyle` via the existing `PlaybackService` covers the wire; explicit channel + verification deferred) all stay open.
- [x] **F.3** Configurable rewind / forward seconds — defaults 30s back / 30s forward; long-press on either button opens a quick selector. Persisted in DataStore. (Voice 25.12.1 added this — common request.) — **shipped in commit `464dbcb`:** new `data/playback/PlaybackSettings.kt` narrow facet (R.A pattern, R.B store split — three `Flow<Int>` + three suspend setters, allowed values 5/10/30/60, coerce-to-default on read AND write). Backed by a dedicated `playback_settings` DataStore-Preferences file (`AppGraph.playbackSettingsDataStore`), separate from `library_ui` so a future Phase K "reset playback" affordance can nuke just this. `PlaybackController` (a) takes `PlaybackSettings` as a constructor param, (b) collects the three Flows into `@Volatile` private fields (`rewindMs` / `forwardMs` / `autoRewindMs`) via three independent `applicationScope.launch { ... }` collectors — cold-path values, no hot-recomposition risk. `TransportCommands.rewind30()` / `forward30()` renamed → `rewind()` / `forward()` (the value isn't always 30s anymore; cleaner API surface than keeping a misleading name); only caller is `PlaybackScreen` so the rename is local. Icon swap from `Icons.Filled.Replay30` / `Forward30` → `Icons.Filled.FastRewind` / `FastForward` (generic glyphs match the variable seconds value). `PlayerTransport` wraps each seek IconButton in a `Box(Modifier.combinedClickable(onClick, onLongClick))` — long-press opens `SeekSecondsPickerDialog` (4 buttons 5/10/30/60, current selection highlighted in `primary`). Selection persists via `playbackSettings.setRewindSeconds(value)` / `setForwardSeconds(value)`. Strings: `player_rewind_30_cd` / `player_forward_30_cd` retired in favor of format strings `player_rewind_cd` / `player_forward_cd` (with `%1$d`); new `player_rewind_picker_title` / `player_forward_picker_title` / `player_seconds_label`. `AndroidPlaybackSettingsTest` covers fresh-store defaults, round-trip for every allowed value, coerce-to-default on out-of-range setter inputs AND on out-of-range persisted reads, and persistence across a recreated facet instance.
- [x] **F.4** Auto-rewind on resume — when resuming after >5 minutes paused, rewind by `autoRewindSeconds` (configurable, default 5s). Implemented in `PlaybackController.onResume`. — **shipped in commit `464dbcb`:** `PlaybackController.play()` overrides the bare `c.play()` path. `Player.Listener.onIsPlayingChanged(false)` records `pausedAtEpochMs = System.currentTimeMillis()`; `play()` reads it, clears it, and if the wall-clock delta exceeds `AUTO_REWIND_THRESHOLD_MS` (5 * 60 * 1000) AND the player is currently not playing AND `autoRewindMs > 0`, issues a `c.seekTo((c.currentPosition - autoRewindMs).coerceAtLeast(0L))` before `c.play()`. The threshold gate uses wall-clock (not media time) because the listener cares about real-world idle, not playback position. The `autoRewindSeconds` flow lives in the same `PlaybackSettings` facet (default 5s), persisted in the same `playback_settings` DataStore file; coerced to one of {5, 10, 30, 60} on read and write. No UI picker for `autoRewindSeconds` in this phase — Phase K's settings catalog will add one (the facet is shaped to accept it).
- [x] **F.5** Chapter list — bottom sheet from the player overflow. Lists chapters with per-chapter progress, current chapter highlighted, tap → seek to chapter start. — **shipped in commit `27a60e3`:** `ui/playback/ChapterListSheet.kt` renders a `ModalBottomSheet` + `LazyColumn` of all chapters. R.A discipline: takes the narrow `ChapterSource` interface (two methods), not `LibraryRepository`. Cold-start-perf A.5: lazy-mount behind `if (chapterSheetOpen) { ChapterListSheet(...) }` at the call site, so the full chapter list never enters the `PlaybackUiState.Loaded` hot path — the sheet's own composable fetches via `observeChaptersForBook(bookId)` only while open. Active row gets `secondaryContainer` background + filled-`PlayArrow` leading icon (m3-expressive surface-tier cue + filled-icon glyph). `LaunchedEffect(chapters.size, currentChapterIndex) { scrollToItem(...) }` so users on chapter 47 don't land scrolled-to-top. Overflow icon swapped from `Icons.Filled.MoreVert` → `Icons.AutoMirrored.Filled.List` and `player_overflow_cd` string retired in favor of `player_chapter_list_cd`. `formatMmSs` extracted from `PlaybackScreen.kt` → `ui/playback/PlayerFormatting.kt` so the sheet's duration column and the scrubber's timestamp row share one formatter.
- [x] **F.6** Background gradient tinted by the cover-art's extracted Palette swatch (Voice does this; mild but adds polish). **Custom chrome tint picker** (tonearmboy `82d6248` analog — a Phase K row that overrides the Palette extraction with a user-picked accent) is a follow-up, not a Phase F gate. — **shipped in commit `4d2a391`:** new `ui/playback/PaletteTint.kt` runs `BitmapFactory.decodeFile(coverPath, { inSampleSize = 4 })` + `Palette.from(bitmap).generate()` on `Dispatchers.IO` (cold-start-perf — never block UI on bitmap decode). Swatch fallback ladder: `vibrantSwatch` → `darkVibrantSwatch` → `dominantSwatch` → null (greyscale covers / decode failures fall back to plain `surface`, gradient becomes invisible). `PlaybackScreen` wraps the outer Column in a `Box` painted with a `Brush.verticalGradient(listOf(animatedTop, surface))`; `animatedTop` is `(tint ?: surface).copy(alpha = 0.4f)` driven by `animateColorAsState` so book switches cross-fade rather than snap. `LaunchedEffect(coverPath) { tint = extractTint(coverPath) }` rekeys on the path, not the whole Loaded state, so the 250ms position ticker doesn't retrigger extraction. `TopAppBar` `containerColor` flipped to `Color.Transparent` so the gradient shows through the top band. Gradient sits over the `NowPlayingSheet`'s own `surface` background — both share the same base so the cross-fade band of the sheet (peek↔full) reveals the tint smoothly. Dep: `androidx.palette:palette:1.0.0` (Apache-2.0, no licensee exemption needed). **Cleanup ridealong:** retired `SelectCoverFromInternet`'s unused `okHttpClient` / `coverStore` parameters + the `retainedPhaseBPickPathRemoved` placeholder fun (cover-art Phase C/D wedge — full-image fetch now lives in `EditCoverDialog`, this composable doesn't need either). `WhisperboyApp.kt` `entry<CoverSearchRoute>` call site drops the two args; `graph.okHttpClient` + `graph.coverStore` retained on AppGraph (consumed by `CoverApiModule` + `BookSource.setCustomCover` respectively).
- [x] **F.7** Notification + lock-screen via `MediaStyle` notification, channel `whisperboy_playback` (IMPORTANCE_LOW, silent). Replaces Phase B's stub. Verified via `dumpsys media_session` and lock-screen screenshots. — **shipped:** F.7 in commit `ca4c8c9`. `PlaybackService.onCreate` registers the `whisperboy_playback` channel (IMPORTANCE_LOW, no badge, name + description from strings.xml) and installs `DefaultMediaNotificationProvider.Builder(this).setChannelId("whisperboy_playback").setNotificationId(1001).build()` so Media3's built-in `MediaStyle` notification lands in our channel rather than the default. AVD lock-screen + `dumpsys media_session` verification deferred (emulator in use by parallel work); the next AVD-friendly task will confirm.

---

## Phase G — sleep timer

Goal: the best sleep timer in class. **Voice analog:** `:core:sleeptimer:impl` (`SleepTimerImpl`, `IsTimeInRange`, `ShakeDetector`, `CreateBookmarkAtCurrentPosition`).

- [ ] **G.1** `SleepTimerService` — modes are sealed `SleepTimerMode`: `Timed(Duration)`, `EndOfChapter`. Default duration persisted in DataStore (`SleepTimerPreference`).
- [ ] **G.2** Sleep timer bottom sheet from the player top-app-bar button. Quick-pick: 5 / 10 / 15 / 30 / 60 minutes + "End of chapter" + custom. Active timer shows remaining time on the button itself (filled state).
- [ ] **G.3** Fade-out: when remaining < `fadeOutDuration` (default 30s), poll every 200ms and ramp `Player.volume` down with a `FastOutSlowInInterpolator`-equivalent curve. After pause, restore volume to 1.0f for next session.
- [ ] **G.4** Shake-to-resume: after the timer pauses, register a `ShakeDetector` (`SensorManager.SENSOR_ACCELEROMETER`) for 30s. On detected shake, resume + restart the timer with the user's last-used duration. Window is tight to avoid false-positives from putting the phone down.
- [ ] **G.5** Auto-bookmark on fire: when the timer pauses playback, write a `Bookmark` with `setBySleepTimer = true`, title = "Sleep timer", time = current position. Surfaces in the bookmark list with a clock icon to distinguish from manual.
- [ ] **G.6** Auto-enable in daily window: settings option to auto-arm the timer between two configurable times (e.g. 22:00–02:00). When in window AND playback starts AND no timer active, arm with the default duration. (`IsTimeInRange` is the unit.)

---

## Phase H — bookmarks

Goal: named bookmarks tied to position-in-book. **Voice analog:** `:features:bookmark`.

- [ ] **H.1** `BookmarkScreen` — list of bookmarks for the current book, grouped by chapter. Each row shows title, time, addedAt, "set by sleep timer" badge if applicable.
- [ ] **H.2** Add bookmark via the player top-app-bar bookmark button. Default title = chapter title + position; user can rename inline.
- [ ] **H.3** Tap → seek to bookmark position + auto-resume.
- [ ] **H.4** Long-press → rename / delete.

---

## Phase I — chapter parsing (M4B + Matroska)

Goal: parse embedded chapter markers from M4B (Apple chap-track + Nero chpl atoms) and Matroska/MKV/WebM (EBML chapters). Read chapters via SAF stream — no temp files. **Voice analog:** `:core:scanner` (`Mp4BoxParser` + visitors `ChapVisitor`/`ChplVisitor`/`MdhdVisitor`/`StcoVisitor`/`StscVisitor`/`SttsVisitor`; `MatroskaChapter`, `MatroskaMetaDataExtractor`, `SafSeekableDataSource`).

This is *the* feature that separates a real audiobook player from a music player pointed at long files. Plan it carefully; budget time.

- [ ] **I.1** `SafSeekableDataSource` — random-access reader over a SAF `Uri` (Media3-style `DataSource`-flavored, but for our parser, not for ExoPlayer). Wraps `ContentResolver.openAssetFileDescriptor` and `seek` on the underlying `FileDescriptor`. Necessary because SAF streams are forward-only by default.
- [ ] **I.2** `Mp4BoxParser` — visitor-based MP4 atom walker. Visit `moov.trak.mdia.minf.stbl.{stco,stsc,stsz,stts}` to build offset tables; visit `moov.udta.chpl` for Nero chapter list; visit `chap` track-reference + corresponding text track for Apple chap-track.
- [ ] **I.3** Apple chap-track decoder — for each chunk in the chap text track, decode `(start_time, length, title_string)`. Convert sample-time to seconds via `mdhd` timescale.
- [ ] **I.4** Nero chpl decoder — straight `(timestamp_in_100ns, title)` list. Convert to seconds.
- [ ] **I.5** `MatroskaChapter` parser — walk EBML, find `\Chapters\EditionEntry\ChapterAtom`, extract `ChapterTimeStart` (ns) + `ChapterDisplay\ChapString`. Skip ordered / nested-edition variants for v1.
- [ ] **I.6** Vorbis comment chapter extractor — `CHAPTER001`, `CHAPTER001NAME`, etc. Less common but cheap to add.
- [ ] **I.7** `ChapterParser` dispatch — by extension + sniffed magic, route to the right parser. Output: `List<ChapterMark>(positionMs, title)`. Falls back to "one mark at 0 with the file name" if nothing parses.
- [ ] **I.8** Wire into Phase D's scanner — `SingleFile` books with parsed chapters get rich `Chapter` rows; without, get one chapter == whole file.

---

## Phase J — speed, skip silence, volume gain

Goal: per-book speed (0.5x–3.5x), skip silence, volume gain in dB. **Voice analog:** `VoicePlayer` + `VolumeGain` audio processor.

- [ ] **J.1** Speed — ExoPlayer's native `setPlaybackSpeed(Float)` (Sonic time-stretching, no FFmpeg). Persisted on `BookEntity`. Settings UX: slider + numeric display + "reset to 1.0x" button. Range 0.5–3.5 in 0.05 steps.
- [ ] **J.2** Skip silence — `Player.skipSilenceEnabled = enabled`. Persisted on `BookEntity`. Toggle in player overflow.
- [ ] **J.3** Volume gain — custom `VolumeGain` `AudioProcessor` measured in `Decibel` (range -3 dB to +12 dB), applied as a pre-amp in the audio pipeline before output. Persisted on `BookEntity`. Settings UX: slider with numeric dB readout. Note: this is *additive* to the system volume, not a substitute.
- [ ] **J.4** Per-book vs global defaults — when a book is first scanned it inherits global defaults (DataStore); subsequent edits write to the `BookEntity` row, not back to the defaults. This is Voice's behaviour and the right one — a quiet book gets +6 dB without the next book screaming.

---

## Phase K — settings screen — partial; K.1 scaffold + K.6 shipped in commit `d4df060`

Goal: full settings tree. **Voice analog:** `:features:settings`.

> **Read [`m3-expressive.md`](m3-expressive.md) before starting.** This is the phase that fully cashes in the M3E plan: the C-series steps (`CategoryAccent` data class, ~5–6 hand-picked accent pairs for whisperboy's category split — Appearance / Library / Playback / Sleep timer / Bookmarks / About — and the `SettingsCategoryIcon` composable wiring filled glyph icons into coloured circular avatars per row). Auto-derive `accent` from row `id` at the row composable (gotcha #3) so any future hand-rolled screen — License sub-page, About — picks up colour for free instead of going monochrome. `RoundedCornerShape(28.dp)` + `defaultElevation = 0.dp` + divider-less `Arrangement.spacedBy(2.dp)` for the in-card row stack. K.6 (About → Open-source licenses link) ties into [`oss-licenses.md`](oss-licenses.md).

- [x] **K.1** `SettingsScreen` — Material 3 settings list. Sections: Playback, Sleep timer, Library, Theme, About. _Scaffold shipped — top-app-bar + grouped cards + five category rows + back nav + library overflow "Settings" entry. Subcategory destinations for Playback / Sleep timer / Library / Theme stub a "Coming soon" snackbar until K.2 / K.3 / K.4 / K.5 land. Row composable deliberately whisperboy-fresh (not ported from tonearmboy's catalog) so the Phase C inch of [`m3-expressive.md`](m3-expressive.md) can drop in `CategoryAccent` coloured circular avatars by editing one composable._
- [ ] **K.2** Playback section: default speed, default skip silence, default gain, rewind/forward seconds, auto-rewind seconds, equalizer launcher (`AudioEffect.ACTION_DISPLAY_AUDIO_EFFECT_CONTROL_PANEL` system intent — same pattern as Voice).
- [ ] **K.3** Sleep timer section: default duration, fade-out duration, shake-to-resume on/off, auto-arm window.
- [ ] **K.4** Library section: list of roots with `FolderType` + remove + add-new; manual rescan button.
- [ ] **K.5** Theme section: light / dark / follow-system; dynamic color toggle (Material You). **Custom chrome tint picker** (tonearmboy `82d6248`) — optional override of the Palette-extracted accent for users who want a fixed app-wide tint instead of cover-driven.
- [x] **K.6** About section: version, build hash, license, link to GitHub repo. Build metadata captured at compile time (same pattern tonearmboy validated). **Clean-room credits clarification** (tonearmboy `765c545`): the About text explicitly states that the relationship with Voice is "spiritual sibling" — UI design-space inspiration only, no code copy, MIT vs GPLv3 — so anyone reading the credits doesn't infer a fork. _Shipped — AboutScreen renders app icon + version + MIT license + GitHub link + clean-room sibling credit + OSS licenses TODO row (wired to placeholder snackbar until [`oss-licenses.md`](oss-licenses.md) ships). Version + git-sha are hard-coded today because `buildConfig = false` in `app/build.gradle.kts`; a follow-up inch will flip `buildConfig = true` + add `buildConfigField` for `VERSION_NAME` / `GIT_SHA` and replace the constants in `AboutScreen.kt`._
_No K.7. Cover-art has no settings surface — see [`cover-art.md`](cover-art.md) "Settings surface" section. Voice doesn't expose cover-art settings, and we don't either; the "Search online" action is per-book in the long-press sheet (Phase E.5)._

---

## Phase L — onboarding flow

Goal: a clean first-run flow that gets the user from install to first book playing. **Voice analog:** `:features:onboarding`.

- [ ] **L.1** Welcome screen — one short sentence + one CTA.
- [ ] **L.2** Permissions screen — `POST_NOTIFICATIONS` (API 33+); SAF permission is granted by the picker, not by a Manifest-level dialog, so this screen is short.
- [ ] **L.3** Folder picker screen — explains the four `FolderType` modes briefly with a small visual for each, then launches the SAF `OPEN_DOCUMENT_TREE` flow, then asks the user to pick the type.
- [ ] **L.4** First-scan loading — shows scan progress (books found / chapters parsed). Skippable to library after first book lands.
- [ ] **L.5** Onboarding-completed flag in DataStore. Re-running clears it (debug menu only).

---

## Phase M — home-screen widget

Goal: now-playing widget. **Voice analog:** `:features:widget`.

- [ ] **M.1** Glance widget (`androidx.glance.appwidget`) — book cover, book title, chapter title, transport (prev / play-pause / next).
- [ ] **M.2** Tap on cover → open the player screen.
- [ ] **M.3** Configurable size variants (1×1, 2×2, 4×1).

---

## Phase N — Android Auto

Goal: the browse tree shows up in Android Auto, voice search resolves to a book and starts playback. **Voice analog:** `:core:playback` (`LibrarySessionCallback`, `BookSearchHandler`, `BookSearchParser`, `VoiceSearch`, `CustomCommand`).

This is the payoff for picking `MediaLibraryService` in Phase B.2 instead of the simpler `MediaSessionService`.

> **M3E note for Phase N (per [`m3-expressive.md`](m3-expressive.md) D.4):** the AAOS car system theme dominates the browse-tree rendering, so the M3E surface ladder doesn't reach inside Auto's chrome. But any custom in-car activity reachable from the car launcher (a settings activity, a confirmation screen) MUST go through the same `MaterialExpressiveTheme` entry — otherwise the car-side surfaces will read flat against the M3E phone-side ones.

- [ ] **N.1** Manifest declarations — `automotiveApp`, allowed-package metadata for `com.google.android.projection.gearhead`.
- [ ] **N.2** `LibrarySessionCallback.onGetLibraryRoot` returns the root browseable node.
- [ ] **N.3** `onGetChildren` walks: Root → "Currently listening" / "Not started" / "All books" / "Authors". Each leaf is a `MediaItem` with a `BookId` that `onPlaybackResumption` can re-hydrate.
- [ ] **N.4** Voice search: parse `query` for artist/album/title — `BookSearchParser` resolves to a `BookId` + position (resume where you left off, not from the beginning).
- [ ] **N.5** Custom commands: set sleep timer, change speed, skip silence — surfaced as Auto custom action buttons.
- [ ] **N.6** Verify via Desktop Head Unit (DHU) on the dev machine (`~/Android/Sdk/extras/google/auto/desktop-head-unit`).

---

## Phase O — release / Obtainium / GitHub Actions

Goal: a sideload-able APK on GitHub Releases. Mirror of tonearmboy's release pipeline almost exactly. **Voice analog:** none — Voice ships via Play + F-Droid.

- [ ] **O.1** `scripts/build-release-apk.sh` — port from tonearmboy, swap `tonearmboy` → `whisperboy` in artefact names + tag prefixes.
- [ ] **O.2** `.github/workflows/release.yml` — tag-only, self-disabling, mirror of tonearmboy's workflow. Zero CI minutes by default.
- [ ] **O.3** First release `v0.1.0-<sha7>` — debug-signed, sideload via Obtainium, validate the install path end-to-end.
- [ ] **O.4** Production-signed releases when keystore is in place — env vars `WHISPERBOY_RELEASE_KEYSTORE` / `WHISPERBOY_RELEASE_KEY_ALIAS` / `WHISPERBOY_RELEASE_KEY_PASSWORD`.
- [ ] **O.5** Wire Baseline Profile generation into `scripts/build-release-apk.sh` (cold-start-perf F.4). Profile generation is the only delta-of-work piece in [`cold-start-perf.md`](cold-start-perf.md) — F.1 plugin + sibling `:baselineprofile` module, F.2 record cold-boot path via `MacrobenchmarkRule`, F.3 `androidx.profileinstaller` in app module, F.4 hook into the release script so every shipped APK includes the profile. Worth more than every other cold-start guard combined (~25–35% off cold start).

---

## Phase P — polish + edge cases (the unglamorous one)

Caught-once-then-fixed bugs and gnarly real-world cases that don't belong to any earlier phase. Whisperboy will accumulate these the same way tonearmboy did; this is the catch-bucket so they don't get filed under a wrong phase.

- [ ] **P.1** Cold-start playback resumption — `MediaSession.Callback.onPlaybackResumption` returns last book + position. Verified via process-death simulation (`adb shell am force-stop com.eight87.whisperboy` while playing).
- [ ] **P.2** Headset / Bluetooth controls — play, pause, prev-chapter (long-press prev), next-chapter (long-press next). Verified on AVD virtual headset + real BT headset.
- [ ] **P.3** Process death during scan — partial scan is committed as a Room transaction per-book; killing the process mid-scan loses only the in-flight book.
- [ ] **P.4** SAF permission revoked under us — when a tree's `Uri` becomes unreadable, all books from that tree are marked `active = false` (soft-delete) and a banner on the library screen offers to re-pick.
- [ ] **P.5** Very large libraries — Voice has #3175 (OOM on huge libraries). Profile whisperboy at 500 books, 10000 chapters; if Room queries on the library screen are blocking, page with `Pager` from `androidx.paging`.
- [ ] **P.6** Foreground service lifecycle on Android 14+ — `FOREGROUND_SERVICE_MEDIA_PLAYBACK` strictness around when the service can start in the background. Verify on API 34+ AVD.
- [ ] **P.7** Position-save cadence — Voice's "experimental playback persistence" (26.4.3) saves on events not continuously, for battery. Mirror the pattern: save on chapter-end, on pause, on backgrounding, on shutdown — not on a 1Hz timer.
- [ ] **P.8** **Skip rescan when nothing changed** (tonearmboy `d0a566d` analog for SAF). Cache the SAF tree fingerprint (document count + max-mtime under each persisted root) in DataStore; on foreground-rescan, compare; if unchanged, no-op. Saves the splash-hold + the scanner pass when the user is just resuming the app with no library deltas.
- [ ] **P.9** **Nav scaffold inset double-consume gotcha** (tonearmboy `5bb2fd2`). When the root scaffold consumes `WindowInsets.statusBars` AND the top app bar also pulls from `LocalWindowInsets`, the status bar gets padded twice — visible as a too-tall top inset on edge-to-edge. Audit once at Phase E, audit again at Phase F's full-screen player. Fix is `Modifier.consumeWindowInsets(...)` at the right level.
- [ ] **P.10** **Two-stage Compose splash NOT recommended.** tonearmboy tried it in `f249b0e` to bypass the Android 12+ system circle clip, and reverted in `51d1769` — the square overlay was visible for ~half a second before fading and "not worth the moving parts". Whisperboy stays on the standard system splash with the 60% mipmap icon (see [`m3-expressive.md`](m3-expressive.md) gotcha #4). This bullet exists so a future agent doesn't re-discover the same regression.

---

## Stretch — post-1.0 differentiators (planned, not scoped to v1)

These are the differentiators identified in the Voice deconstruction (`docs/plans/sharing-analysis.md` and the README "non-goals" list). **Not v1.** Listed here so they don't fall out of the surface area when v1 ships.

- **S.1** Cross-device position sync — optional, BYO-server (WebDAV / Nextcloud) or first-class Audiobookshelf integration. Top recurring community ask on Voice.
- **S.2** Series detection + smart shelves — auto-detect series from filename / metadata, group, "next in series", listening stats.
- **S.3** Standalone Wear OS app — offline-sync a chapter to the watch, leave the phone behind.
- **S.4** Chromecast — Cast a book to a speaker.
- **S.5** Smart-resume — when resuming after >24h, rewind to the last sentence boundary via on-device VAD (whisper-tiny or simpler).
- **S.6** OPDS catalogue browsing for public-domain audiobooks (LibriVox, Standard Ebooks).
- **S.7** Chapter editor — inline rename / split / merge of chapters, write-back as a sidecar `chapters.txt` (no mutation of the original M4B).

---

## Subagent dispatching template

Each subagent prompt for a whisperboy phase should include:

```
You are picking up Phase <X> of whisperboy's main build plan.

Read these files first, in order:
1. /home/laragana/workspace/whisperboy/CLAUDE.md
2. /home/laragana/workspace/whisperboy/docs/plans/main.md (find Phase <X>)
3. /home/laragana/workspace/whisperboy/docs/plans/sharing-analysis.md (skim — relevant if your phase touches a candidate-shareable surface)
4. /home/laragana/workspace/whisperboy/docs/plans/cold-start-perf.md (REQUIRED for any UI-touching phase — E, F, G's bottom sheet, K's settings list, L's onboarding, M's widget — preventive guards, not optional)
5. /home/laragana/workspace/whisperboy/docs/plans/m3-expressive.md (REQUIRED for any UI-touching phase — Phase E lands the M3E foundation, Phase K cashes in the catalog avatars, Phase N propagates the theme into AAOS surfaces; gotchas #1–#7 prevent re-running tonearmboy's discovery cycle, including the top-app-bar 32dp default and the do-not-ship-two-stage-Compose-splash anti-pattern)
6. /home/laragana/workspace/whisperboy/docs/plans/cover-art.md (REQUIRED for Phase E — library grid mounts first CoverArt consumer; REQUIRED for Phase K — service-picker + threshold-slider + bulk-scan settings land there; REQUIRED for Phase F — full-size cover surface). Five locked rules including "no web requests by default" — non-negotiable.
7. The Voice analog if mentioned in the phase header (browse-only via WebFetch — do not vendor any code).

Your job:
- Land sub-steps <X.1> through <X.N>.
- Tick each `- [ ]` to `- [x]` IN THE SAME COMMIT that ships the work.
- Add `**Shipped:** <X.1>–<X.N> in commit <id>.` to the phase footer when all sub-steps are ticked.
- Do not opportunistically refactor other phases.
- Do not modify ~/.claude/ files.
- Consult `android docs search <q>` and the `android-skills` MCP before general web search for Android API questions.
```
